# MEX 0.8.2 setup release

Implementation branch: `codex/0.8.2-hub-setup`. This document describes the branch changes; the release is not published.

## Accepted scope

- Browser setup is the default for `mex setup`; bare `mex` opens Hub or setup. `setup --cli`, `tui`, and the read-only `setup --dry-run` keep terminal access. Browser flags and explicit/saved mode selection are preserved.
- The same scaffold, integration, identity, scan, Graph, population, and Wiki finalization phases remain in use. Agent failures retain the real manual prompt. Integration notes appear as advisory guidance.
- Exact Git review and explicit local commit remain required. After committing, show a completion guide; **Open Hub** performs promotion. Failed opening cannot repeat the successful commit. The existing Hub tour remains separate.
- Offer optional global installation at the running package version, with verification, bounded subprocess ownership, progress, retry, copy, and skip. No automatic installation or privilege escalation.
- Offer optional email/name contact using the existing Web3Forms service. Email is required only for submitting, name is optional. Store only submitted/skipped markers under the user's global `.mex/setup/`, and share that preference with Overview. No contact data goes into the repo or telemetry.
- Update commands, README translations, setup template/diagram, privacy guidance, package metadata, capability goldens, and release notes for 0.8.2.

The fresh-project interview remains deferred. No PostHog contact profiles or new tour are introduced.

## Validation

- Production build and final TypeScript checks pass locally on Node 22.17.1/macOS.
- Full Hub-web suite: 32 files, 546 tests passed with two workers.
- Full root suite: 247 files passed and 10 failed in the initial four-worker run (3,927 tests passed, 67 failed, 19 skipped, plus two worker RPC timeouts). The new installer's temporary cleanup was added to the filesystem-writer audit. All affected suites then passed serially with unchanged time limits: 32 CLI tests and 333 tests across the other nine files. The broad run itself was not clean; normal CI remains a release gate.
- Focused command, setup lifecycle, contact, installer, HTTP, completion, and existing contact-dialog tests cover the changed paths. Tests retain the actual manual prompt after agent failure and exercise install cancellation, output/deadline limits, request authorization, and private preference markers.
- Built browser scenarios: both agent activity/reload/cancel cases and both commit/completion/Open Hub cases passed. The stale-review scenario uses an existing scaffold to focus its repeated review on changed files; the full new-scaffold scenario retains complete diff coverage. Browser test limits were not increased.
- Packed-install smoke passed, including the local 0.8.2 tarball, terminal setup through `--cli`, bare-CLI Hub startup, packaged assets, official agent skills, and public declaration checks.
- Visual inspection: the completion screen fits the Hub's minimum 1024-pixel viewport. The existing desktop-width notice remains below that width. The README SVG was checked as valid XML; it was not rendered in the browser.
- Manual review: the user completed setup and explored the Hub in a disposable bookmarks project. Global installation and contact submission were simulated; the temporary Hub was stopped afterward.
- The existing Windows/macOS portability job includes the new contact, global-installation, and owned-process tests.
- Network sends are stubbed in contact tests. No real contact submission or machine-wide installation is performed for validation.
- Public root exports and Graph/Wiki/Relay schemas remain unchanged. Internal setup contracts add completion actions and contact preferences.
- Existing MEX graph context was stale for this branch. Current-source inspection was used; old grounding baselines were not renewed from stale evidence.

## Repository verification checklist

1. Public API: `src/index.ts` is unchanged; emitted declarations and packed exports pass their checks.
2. Read/write authority: preference and installation reads remain non-mutating. Explicit actions retain existing Hub authorization/CSRF gates, contained writes, reviewed Git revisions, and recoverable failures.
3. Bounded work: contact input/response/deadline and installer output/deadline/cancellation checks pass; stored contact markers contain no personal data.
4. Tests/types: focused tests, all 546 Hub-web tests, final typecheck, and serial reruns of every affected root suite pass. Initial broad-run timing/RPC failures are recorded above.
5. Packaging/assets: production build, packed-install smoke, and changed browser flows pass. Graph evaluator/protocol code is unchanged, so evaluator tests were not rerun.
6. Diff hygiene: `git diff --check` passes. Generated databases, local state, build output, and browser artifacts are excluded from the branch changes.
7. Protocol compatibility: Graph/Wiki protocol shapes, stable errors, ordering, and cursors are unchanged. Existing contract tests remain in place, including the successful serial regression checks above.

## PR #197 performance follow-up

The initial PR head `848a1b1038c6b2048b10fcdab1a10a4c3fbb2b58` passed Node
22.22.0/24, macOS/Windows portability, and browser CI in
[run 34758778604](https://github.com/mex-memory/mex/actions/runs/34758778604).
Release performance stopped immediately on three deterministic asset failures:
initial CSS 70,919 > 70,902 bytes; setup JS 116,725 > 107,648 bytes; setup CSS
22,553 > 21,081 bytes. Runtime crossings in that report did not receive an
independent confirmation because asset failures stop the gate first.

The preceding main head `dcc0a0471b85b96333d8e41aa18d0128e384ccfe` also failed
release performance in
[run 34745420673](https://github.com/mex-memory/mex/actions/runs/34745420673).
Its final material failure was medium-profile Wiki rebuild time, 408.675 ms
against 301 ms after independent confirmation. That is historical evidence,
not a confirmed runtime failure on the corrected PR head.

The fix separates private contact schemas into their own entry so Overview
preference reads never load setup. Shared `HUB_LIMITS` move unchanged to a leaf
module, avoiding the setup bundle's duplicate eager construction of the full
Hub schema barrel. Existing internal exports remain available. Source tests
reject either dependency regression, production build assertions protect the
contact boundary, and the built browser test checks actual contact/setup asset
requests.

Tailwind was emitting `.block` solely from a setup comment. Rewording the comment
restores initial CSS to main's exact 70,898 bytes. Setup CSS growth is the
accepted completion guide and advisory UI. The setup-only calibration retains
all measured files and SHA-256 hashes in
`scripts/release-benchmark/setup-asset-budget.json` and includes the new dynamic
contact contract. Local production bytes are valid deterministic asset evidence;
no local timing or memory values supply release limits.

| Setup assets | Measured bytes | Limit: ceil(bytes × 1.05) |
| --- | ---: | ---: |
| JavaScript, including contact | 71,143 | 74,701 |
| CSS | 22,553 | 23,681 |
| Fonts beyond the shell | 0 | 0 |

This lowers the setup JavaScript limit from 107,648 bytes and updates only the
setup stylesheet allowance for the added UI. Every shared, route, runtime,
heap, RSS, and maintenance limit in `budgets.json` stays byte-for-byte unchanged.
The deterministic asset gate passes. Validation on the fix includes 97 focused
root tests, a final rerun of all 50 affected benchmark/boundary tests, and
TypeScript checks. The full Hub-web run passed 548 tests; one new test expected
plain headers instead of the transport's `Headers` object. Correcting that
assertion made all three new contact-client tests pass on rerun. The production
Overview request/idle proof and both built setup commit/completion scenarios
pass (three browser checks). The packed install and all retained asset hashes
pass. An earlier parallel packaging/browser attempt raced the packaging
prepare-build against browser asset reads; the three browser scenarios passed
serially after packaging finished, without changing product or test limits.

The corrected head still requires normal pinned CI enforcement, including
fresh-runner confirmation where indicated.

## Remaining release gates

- Review the PR and merge after the required checks pass.
- Run the normal Node 22/24, Windows/macOS portability, full browser, and pinned Linux release-performance CI on the reviewed head. Do not recalibrate budgets from laptop results.
- Finalize the release date, then publish/tag 0.8.2 through the normal release workflow.
